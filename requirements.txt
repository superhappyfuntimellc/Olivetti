# Web App dependencies (Streamlit)
streamlit>=1.28.0
python-docx>=1.0.0

# Core AI dependencies (both CLI and Web)
openai>=1.0.0
anthropic>=0.25.0
requests>=2.31.0

# Optional: For desktop mode (native window instead of browser)
# Uncomment to install:
# pywebview>=4.0.0

# Development dependencies (optional)
# pytest>=7.4.0
# black>=23.0.0
# ruff>=0.1.0
